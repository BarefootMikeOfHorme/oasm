[package]
name = "ipc_bridge"
version = "0.1.0"
edition = "2021"
description = "IPC bridge DLL for OASM runtime"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"
