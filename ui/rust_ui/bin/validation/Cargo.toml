[package]
name = "validation"
version = "0.1.0"
edition = "2021"
description = "Validation DLL for OASM runtime"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
regex = "1.10"
tracing = "0.1"
tracing-subscriber = "0.3"
