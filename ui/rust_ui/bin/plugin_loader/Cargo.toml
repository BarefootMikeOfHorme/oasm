[package]
name = "plugin_loader"
version = "0.1.0"
edition = "2021"
description = "Dynamic plugin loader DLL for OASM runtime"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
libloading = "0.8"
tracing = "0.1"
tracing-subscriber = "0.3"
