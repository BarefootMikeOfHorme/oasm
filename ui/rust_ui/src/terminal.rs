pub fn show_terminal() {
    println!("DEBUG: UI terminal pane (bare-bones)");
}

// Full course meal ideas:
// - Render ANSI-aware terminal widget
// - Support command history + search
// - IPC integration with OASM runtime
